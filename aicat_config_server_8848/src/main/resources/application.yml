server:
  port: 8848
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8001/eureka/
  instance:
    prefer-ip-address: true
spring:
  application:
    name: AICAT_CONFIGSERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wsaperlin/aicat_config.git
          username: wsaperlin
          password: ws2perlin.